"use strict";
// Interfaces & Type Aliases
function createUser(user) {
    // user.id
}
createUser({ id: 1, name: "Yash", email: "test@example.com" });
function isAdmin(user) {
    // user.admin
    // user.id
}
function abc(obj) {
    // obj.id
    // obj.name
    // obj.email
}
let val = 12;
val = null;
function wxyz(obj) {
    //...
}
wxyz(12);
/*************************************************************************/
// Union
let ax;
ax = 12;
ax = "hello";
function abcAdmin(user) {
    // user.id
    // user.name
    // user.email
    // user.getDetails
}
